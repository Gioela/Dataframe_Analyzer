This project gives a new way to encode the objects' column ins a dataset.

The idea is to find a way to encode each categorical value during the creation model phase and to use the same encoding at prediction time.